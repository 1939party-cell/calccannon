# CalcCannon — Your Autopilot Site (Final, Fresh Build)

- Owner wallet (Solana): **5GpN4JNYp7ruijrYL9sYtzcj965exn2pensyQT2WaPmY**
- Cash App tips: **$maddabbs20**

Quick deploy: Use Cloudflare Pages (free). Build command `npm ci && npm run build`, output `dist`.
Set env vars: `JWT_SECRET`, `RPC_SOLANA` (`https://api.mainnet-beta.solana.com`), `SITE_CONFIG` (paste config/site.json).
